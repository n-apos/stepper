package com.napos.stepper.compose.screen

import androidx.lifecycle.ViewModel

/**
 * A base ViewModel for a [MilestoneScreen].
 *
 * This class extends [ViewModel] and is intended to be subclassed to provide UI-related data
 * and handle the business logic for a specific milestone in the stepper.
 */
public abstract class MilestoneViewModel : ViewModel()